# Copy to .env.local — never commit secrets

DATABASE_URL="postgresql://..."
# If you use a pooled URL (e.g. Neon) and migrations fail, add directUrl in prisma/schema.prisma
# and set DIRECT_DATABASE_URL to the non-pooled connection string.

# Garmin OAuth (same flow for production or Garmin Developer / eval apps — swap credentials per environment)
GARMIN_CLIENT_ID=
GARMIN_CLIENT_SECRET=

# Garmin Training API auth mode: bearer (default) or oauth1
GARMIN_TRAINING_AUTH_MODE=bearer
# OAuth1 mode only:
# GARMIN_TRAINING_OAUTH_CONSUMER_KEY=
# GARMIN_TRAINING_OAUTH_CONSUMER_SECRET=
# GARMIN_TRAINING_OAUTH_TOKEN_SECRET=

# Server-to-server: GoFastCompany training-engine dashboard → these routes require the same secret on both apps.
# Header: x-gofast-training-engine-secret
GOFAST_TRAINING_ENGINE_SECRET=
